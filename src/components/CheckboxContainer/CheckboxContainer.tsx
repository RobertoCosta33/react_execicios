import { CCheckbox } from "./CheckboxContainer.styled";
import { Checkbox } from "../Checkbox/Checkbox";

export const CheckboxContainer: React.FC = () => {
  return (
    <CCheckbox>
      <Checkbox />
    </CCheckbox>
  );
};
